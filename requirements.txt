# Prism 依赖
# 核心服务系统无第三方依赖（纯标准库 + urllib）

# ── 屏幕模块（需要树莓派 + SPI 屏幕）──
Pillow>=9.0          # 图像渲染（屏幕显示/摄像头）

# ── 可选：股票监控 ──
# akshare            # A股数据
# pandas             # 数据处理

# ── 可选：小红书发布 ──
# selenium           # 浏览器自动化
# requests           # HTTP 请求
